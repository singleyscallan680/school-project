<?php
// Define variables
$variable1 = "value1";
$variable2 = "value2";

// Check if both variables are defined
if (isset($variable1) && isset($variable2)) {
    // Code to be executed if both variables are defined
}

// Output the result
echo "The two variables $variable1 and $variable2 have been checked.";
?>
